import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  globalMargin: {
    marginHorizontal: 20,
  },

  title: {
    fontSize: 35,
    marginBottom: 20,
    fontWeight: 'bold',
  },
});
